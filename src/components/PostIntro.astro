---
import { getI18N } from '../i18n/'

const { currentLocale } = Astro 
const i18n = getI18N({ currentLocale })
---

<section class="relative z-10 text-xl text-center px-6 sm:px-20 max-w-[80ch] text-pretty mx-auto my-24">
    <h1 class="text-5xl mb-10 text-center text-wrap mx-auto tracking-wide uppercase">
        <p><strong class="font-bold">{i18n.INTRO_TEXT_1}</strong>{i18n.INTRO_TEXT_2}</p>
    <h1/>
</section>